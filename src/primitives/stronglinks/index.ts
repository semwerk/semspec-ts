/**
 * StrongLinks module - Semcontext StrongLink Protocol v1.0
 *
 * ID-based linking that survives refactoring.
 *
 * @module stronglinks
 */

export { Registry, createRegistry } from './registry.js';

export type { LinkTarget, LinkRegistry } from './types.js';
